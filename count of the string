#include<stdio.h>
#include<string.h>
void main()
{
    char a[200];
    printf("Enter the sentence");
    gets(a);
    int count=0;
    int len=strlen(a);
    for(int i=0;i<len;i++)
    {
        if(a[i]==' ')
            count=count+1;
    }
    printf("The number of words is %d",count+1);
}


output:
nter the sentencedfkjg dlgheuoig
The number of wordsáisá2
